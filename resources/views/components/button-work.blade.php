<a id="button_work" class="buttonWork" href="#application_form">
    @if (app()->getLocale() === 'en')
    to Order form...
    @endif
    @if (app()->getLocale() === 'de')
    zum Bestellformular...
    @endif
</a>
